# Quickstart target

This directory is a safe, deterministic target for the public-alpha read-only
analysis command. It has no dependencies and performs no writes or network calls.

From the repository root:

```console
kodiak analyze analyze examples/quickstart --json
```
